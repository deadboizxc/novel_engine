package com.deadboizxc.tuinovel

import android.app.Application

/**
 * =============================================================================
 * TUI Novel - Application класс
 * Чистый Kotlin движок (без Python)
 * =============================================================================
 */
class TuiNovelApp : Application() {
    
    override fun onCreate() {
        super.onCreate()
        // Kotlin движок не требует дополнительной инициализации
    }
}
